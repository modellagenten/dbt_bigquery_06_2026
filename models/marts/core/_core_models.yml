version: 2

models:
  - name: dim_products
    description: "Produkt, angereichert um Verkaufskennzahlen, wie Gesamtanzahl der verkauften Produkte, Gesamterlös, Erstes und Letztes verkauftes Produkt."
    columns:
      - name: product_id
        data_tests:
          - unique
          - not_null
      - name: category
        data_tests:
          - not_null          
