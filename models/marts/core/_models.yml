version: 2

models:
  - name: dim_users
    description: "Users."
    columns:
      - name: user_id
        description: "User Id."
        tests:
          - unique
          - not_null  
      - name: email
        tests:
          - not_null
      - name: first_order_at
        tests:
          - not_null



